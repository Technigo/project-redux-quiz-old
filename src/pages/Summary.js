import React from 'react';
import { ResetButton } from 'components/ResetButton';

export const SummaryPage = () => {
  return (
    <div>
      <h1>Summary Page</h1>
      <p>import score, import button to restart, import some celebration</p>
      <ResetButton />
    </div>
  )
}